# not working
db:
  image: mysql
  env_file: mysql.env
  volumes:
    - libreqdadb:/var/lib/mysql
web:
  build: .
  ports:
    - "8070:3000"
  volumes:
    - libreqdasrc:/usr/src/libreqda
  # verificar persistencia
  links:
    - db
